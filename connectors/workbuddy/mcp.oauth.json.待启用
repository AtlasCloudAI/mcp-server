{
  "_说明": "这是 OAuth 方案的 mcp.json。现在还不能用，卡点见 README.md。auth.atlascloud.ai 支持动态注册并放行 WorkBuddy 回调之后，把本文件改名为 mcp.json、删掉 token-schema.json、并把 connector-meta.json 里的 auth_mode 字段整个删掉（省略即走标准 MCP OAuth 流程）。",
  "mcpServers": {
    "atlas-cloud": {
      "type": "streamableHttp",
      "url": "https://mcp.atlascloud.ai/mcp",
      "timeout": 30000
    }
  }
}
